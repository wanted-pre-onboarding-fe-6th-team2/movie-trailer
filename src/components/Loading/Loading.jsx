import { LoadingContainer, LoadingSpinner } from '@/components/Loading/Loading.styled';

const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingSpinner />
    </LoadingContainer>
  );
};

export default Loading;
